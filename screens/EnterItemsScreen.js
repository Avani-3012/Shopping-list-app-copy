import React, { Component } from "react";
import {
  View,
  Text,
  TextInput,
  StyleSheet,
  TouchableOpacity,
  Image,
  Alert,
} from "react-native";

export default class EnterItemsScreen extends Component {
    render() {
    return (
      <View>
       Enter The items that you need on daily basis.
      </View>
    );
  }
}